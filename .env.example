# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=geekery
DB_PASSWORD=your_secure_password_here
DB_NAME=geekery_db

# Server Configuration
SERVER_PORT=8080
ENV=development

# Logging
LOG_LEVEL=info

# JWT Configuration (REQUIRED - minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_at_least_32_characters_long_here

# Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. JWT_SECRET must be at least 32 characters long
# 3. Never commit your .env file to version control
# 4. For production, use strong random values for JWT_SECRET
